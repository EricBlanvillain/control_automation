# --- LLM API Keys (Required based on chosen models) ---
OPENAI_API_KEY="sk-..."      # If using OpenAI models (ControllerAgent, GraderAgent)
# ANTHROPIC_API_KEY="sk-..." # If using Anthropic models

# --- Agent Configuration (Optional - Defaults provided in code) ---
# PROMPTS_DIR="prompts"                 # Directory containing control prompt JSON files (relative to backend/)
# REPORTS_DIR="reports"                 # Directory where output reports are saved (relative to backend/)
# CONTROLLER_LLM_MODEL="gpt-4.1-mini"   # Agno model ID for the ControllerAgent
# GRADER_LLM_MODEL="gpt-4o-mini"        # Agno model ID for the GraderAgent
# EXTRACTOR_CHUNK_SIZE=2000             # Target chunk size for text extraction (characters)
# EXTRACTOR_CHUNK_OVERLAP=200           # Overlap between text chunks (characters)
# KNOWN_META_CATEGORIES="KYC,RGPD,LCBFT,MIFID,RSE,INTERNAL_REPORTING" # Comma-separated list for Orchestrator

# --- Agno Framework ---
# AGNO_DEBUG_MODE="false" # Set to "true" to enable verbose debug logging for Agno LLM calls in Controller/Grader

# --- Backend API Server ---
# HOST="127.0.0.1"
# PORT=8000

# --- Frontend Configuration (If needed via backend proxy/settings) ---
# Example: REACT_APP_API_URL="http://127.0.0.1:8000"
